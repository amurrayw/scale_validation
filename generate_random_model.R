

generate.measurement.model <- function(n.latents, n.measures.per.latent, n.impurities=list(n.sharing.latent=0, n.output.output.edges=0)){}

create.latent <- function(latent.name, n.measures){}

connect.latents <- function(latent.cause, latent.effect){}


add.impurities <- function(latent.1, latent.2, shared.latent, output.output.edge){}

